import { it, expect, describe, test } from "vitest";
import dataProfile from "../dataProfile";

describe("dataProfile", () => {
  it("accept path string to json and return object with ", () => {
    const result = dataProfile("./");
  });
});
